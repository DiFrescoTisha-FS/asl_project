{# {% extends 'layouts/base.twig' %}

{% block main_content %}
<h2>About {{ variant.name }}</h2>
    <ul style='list-style: none;'>
        <li>
            <label>Id: </label> {{ variant.id }}
        </li>
        <li>
            <label>Name: </label> {{ variant.name }}
        </li>
        <li>
            <label>Price: </label> {{ variant.price }}
        </li>
    </ul>
    <p>
        <a href="/variants/{{ variant.id }}/edit">Edit</a> | <a href="/variants/{{ variant.id }}/delete">Delete</a>
    </p>
{% endblock %} #}

{% extends 'layouts/base.twig' %}

{% block main_content %}
    <h2>About {{ variant.title }}</h2>
    <ul style="list-style: none;">
        <li>
            <strong>Id: </strong> {{ variant.id}}
        </li>
        <li>
            <strong>Title: </strong> ${{ variant.title }}
        </li>
        <li>
            <strong>Slug: </strong> {{ variant.slug }}
        </li>
        <li>
            <strong>Description: </strong> {{ variant.description }}
        </li>
        <li>
            <strong>Price: </strong> {{ variant.price }}
        </li>
        <li>
            <strong>Inventory: </strong> {{ variant.inventory }}
        </li>
        <li>
            <strong>Variant Id: </strong> {{ variant.variantId }}
        </li>
    </ul>
    <p>
        <a href="/variants/{{ variant.id }}/edit">Edit</a> | <a href="/variants/{{ variant.id }}/delete">Delete</a>
    </p>
{% endblock %}