{# {% extends 'layouts/base.twig' %}

{% block main_content %}
<h1>Coming from templates/views/variant/index.twig</h1>
    <h2>All Variants</h2>
    <ul>
        {% for variant in variant %}
        <li>
        
            {{ variant.title }} ${{ variant.price }} -
            <a href="/variants/{{ variant.id }}/edit">Edit</a>
            |
            <a href="/variants/{{ variant.id }}/delete">Delete</a>
        </li>
        {% endfor %}
    </ul>

    <a href="/variants/new">Create Variant</a>
{% endblock %} #}

{% extends 'layouts/base.twig' %}

{% block main_content %}
<div class="m-12">
	<h2 class="mb-2 font-bold">All Variants:</h2>
	<p class="mb-2">template/views/variants/index.twig</p>
	<ul class="mb-2">
		{% for variants in variant %}
			<li class="text-bold mb-2">
				{{ variants.title }} ${{ variants.price }} - 
				<a class="font-bold" href="/variants/{{ variants.id }}/edit">Edit</a> | <a class="font-bold" href="/variants/{{ variants.id }}/delete">Delete</a>
			</li>
		{% endfor %}
	</ul>
	<a class="font-bold mb-2 underline mt-2" href="/variants/new">Create Variant</a>
</div>
{% endblock %}